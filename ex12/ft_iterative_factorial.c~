#include <stdio.h>

int ft_iterative_factorial(int nb)
{
  int i;
  int fact;
  
  i = nb;
  while (i > 1)
    {
      fact = nb * (i - 1);
      i--;
    }
  return(fact);
}
int main(void)
{
  int a;
  a = 3;
  printf("%d", ft_iterative_factorial(a));
  return(0);
}
