#include <stdio.h>
int	main(int argc, **char argv)
{
  int i;
  i = 1;
  while(i < argc - 1)
    {
      if(ft_strcmp(argv[1], ft_strcmp(argv[i + 1], argv[i+2])) > 0)
      printf("%s", ft_sort(argv[i]));
      else
	printf("%s", argv[i + 1])
      
    }
  return(0);
}
