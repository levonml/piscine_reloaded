int	ft_recursive_factorial(int nb)
{
  if (nb < 0)
    return(0);
  if (nb = 0)
    return(1);
  else
    {
      nb = nb * ft_recursive_facorial(nb - 1);
      return(nb);
    }
}

int	main(void)
{
  int nb;

  nb = 4;
  printf("nb! =  %d\n", ft_recursive_factorial(nb));
  return (0);
}
